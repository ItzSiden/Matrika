কাজ ফ্যাক্টরিয়াল(n) {
    যদি n <= 1 {
        ফিরাও 1
    } নাহলে {
        ফিরাও n * ফ্যাক্টরিয়াল(n - 1)
    }
}

বল(ফ্যাক্টরিয়াল(5))   # 120
বল(ফ্যাক্টরিয়াল(7))   # 5040

recursion is taking too long. (Larger Factorials)
var updating not working. (i + 1), (i++)
scope: temp.matrika:8: error: unknown statement: 'দেখাও'




5️⃣ Refactor ASTNode (Optional but Good)

Instead of generic:

char text[MAX_STR];
double num;
int bval;

Use union:

union {
   double num;
   char str[];
   ...
}

Cleaner and more professional.

6️⃣ Improve Error Recovery (Nice Upgrade)

Right now die() exits immediately.

Later:

Implement panic-mode parsing

Continue parsing after minor error

Not urgent.

7️⃣ Add Basic Standard Library

Built-ins:

ইনপুট()

দৈর্ঘ্য()

টাইপ()

Makes it usable for learners.

8️⃣ Add Versioning

Inside main:

Print:

Matrika v1.0

Keep version discipline.